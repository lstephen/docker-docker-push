build: .

default: build

environment:
  - DOCKER_PUSH_NAME=lstephen/docker-push

volumes:
  - /var/run/docker.sock:/var/run/docker.sock

targets:
  build: build

  release:
    volumes:
      - $GIT_SSH_KEY:/ssh/id_rsa
    run: build release

